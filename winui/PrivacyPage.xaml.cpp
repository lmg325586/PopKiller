#include "pch.h"
#include "PrivacyPage.xaml.h"
#if __has_include("PrivacyPage.g.cpp")
#include "PrivacyPage.g.cpp"
#endif

using namespace winrt;
using namespace Microsoft::UI::Xaml;

namespace winrt::winui::implementation
{
    PrivacyPage::PrivacyPage()
    {
        InitializeComponent();

        PrivacyText().Text(LR"(
PopKiller 隐私声明
适用版本：Beta 0.8 ｜ 生效日期：2026 年 9 月 25 日

1. 概述
PopKiller 是一款完全在本地运行的弹窗拦截工具。我们的设计原则是：仅在实现拦截功能所必需的最小范围内处理信息，且默认不将任何数据发送至互联网。本声明说明本工具实际接触、存储和传输的全部信息。

2. 运行期检测与本地存储
为判断窗口是否为弹窗，本工具在运行时会读取目标窗口的以下属性：进程名称、可执行文件路径、窗口标题、窗口类名、窗口样式与层级、进程签名状态，以及由本机启发式算法与机器学习模型计算出的特征评分。

上述信息仅写入你计算机上的本地文件，不会自动上传：
· blocklog.txt：拦截/放行/监控记录，含进程名、窗口标题、类名、启发式特征与模型评分；
· rules.json：你配置的黑/白名单与规则；
· labels.json：你在日志页所作的标注缓存（用于本机导出训练样本）；
· winui.ini：程序设置。

在 zip 便携模式下，以上文件默认位于程序可执行文件所在目录。

机器学习推理完全在本机进行。本工具内置的模型（ONNX 格式）在你电脑上离线运行，不会将窗口内容、特征或任何检测结果发送到外部服务器用于“云端识别”或“在线训练”。

3. 网络通信
本工具唯一会主动发起网络请求的场景是「社区规则库」功能：
· 当你开启社区规则库时，程序会从公开内容分发域名 raw.githubusercontent.com 下载社区共享的规则文件及其 SHA-256 校验值，用于校验完整性。
· 该下载请求会向托管方暴露：你的公网 IP 地址、请求时间、客户端标识（User-Agent）。
· 该请求仅为下载，不会上传你的任何本地数据、规则、日志或窗口信息。
· 若你关闭社区规则库，本工具在运行期间不会发起任何网络请求。
· 如不希望产生此类请求，请在设置中关闭「社区规则库」开关，或在防火墙中阻止本程序联网。

4. 本工具不收集的内容
· 无遥测、无使用统计埋点、无崩溃上报、无分析 SDK；
· 无账号体系，不要求登录；
· 不读取你的文件内容、剪贴板、浏览历史、聊天记录、键盘输入；
· 不收集个人信息以用于广告或用户画像。

5. 行为与权限说明
· 当窗口命中黑名单或启发式阈值时，本工具会向该窗口发送关闭指令并将其隐藏。本工具不会自动强制结束任何进程。
· 在「仅记录」模式下，本工具只写日志，不执行任何关闭或隐藏动作。
· 拦截通知中提供的「加入白名单」「查看日志」等操作，均为本机操作，不涉及数据上传。
· 若启用「开机自启动」，本工具会在本机写入自启动配置（指向程序自身路径），该配置不上传。
· 请谨慎添加黑名单规则，以避免误关正常程序窗口。

6. 数据留存与删除
· blocklog.txt 达到约 1 MB 后会被截断重写，仅保留较新记录；
· 关闭程序不会自动删除上述本地文件；
· 你可随时手动删除程序目录下的上述文件，或使用日志页的「清空日志」功能；
· 「导出训练数据」生成的是本地文件，是否将其分享给他人（例如提交规则库或训练集）完全由你决定，本工具不会自动发送。

7. 第三方与开源
· 社区规则库的托管方为 GitHub，其对你 IP 等信息的处理遵循 GitHub 自身的隐私政策，本工具无法控制；
· 本工具基于 MIT 许可证开源，全部数据处理逻辑均可在源码中审计，不存在隐藏的数据上传行为。

8. 声明的变更
若未来版本新增任何网络通信或数据处理行为，我们将在更新说明与本声明中同步披露，并在显著位置提示。)");
    }

    void PrivacyPage::BackButton_Click(IInspectable const&, RoutedEventArgs const&)
    {
        if (Frame().CanGoBack())
        {
            Frame().GoBack();
        }
    }
}
